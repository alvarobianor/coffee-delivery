import styled from 'styled-components';

export const ContainerLayout = styled.div`
    display: flex;
    flex-direction: column;

    padding: 0rem 10rem;

    min-width: 50rem;
    min-height: 31.25rem;
`;
